@model KonferansPortal.Models.Paylasim
@{
}

<h1>PaylasimEkle</h1>
<form method="post" asp-action="PaylasimEkle" enctype ="multipart/form-data">
    <div>
        <label asp-for="Title">Başlık</label>
        <input asp-for="Title" />
    </div>
    <div>
        <label asp-for="Content">İçerik</label>
        <input asp-for="Content" />
    </div>
    <div>
        <label>Dosya</label>
        <input id="fileSelector" type="file" name="ContentFile" class="form-control-file" onchange="importToHidden()" />
        <input id="hiddenInput" type="hidden" asp-for="ContentFile" />
    </div>
    <button type="submit">Paylaşım Ekle</button>
</form>

<script>
    function importToHidden() {
        var fileSelector = document.getElementById("fileSelector");
        var hiddenInput = document.getElementById("hiddenInput");

        if (fileSelector.files.length > 0) {
            //hiddenInput.value = fileSelector.files[0].name; //not implemented on model yet
        }
    }
</script>